
<h2>Sign up</h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true %>
  </div>

  <!--<div id="user_extended_profile">
    <%= f.label :role %><br />
    <%= f.select :role, options_for_select([["Select One", ""], "Student", "Faculty", "Collaborator"]), :class => 'genForm_dropBox', id: 'user_extended_profile' %>
  </div>

  <div class="js-dependent-fields[data-select-id='user_extended_profile' data-option-value='Student']">
<%= f.email_field :email, autofocus: true %>

</div>
  <div class="js-dependent-fields"  data-select-name = "user_extended_profile" data-option-value = "Student" %>
    <%= f.fields_for :student do |s| %>
  <%= s.label :rollNumber %>
  <%= s.text_field :rollNumber %>
  <% end %>

</div>
-->
<div id ="student_profile">
<%= f.select :role, ['Student', 'Faculty', 'Collaborator'] %>
</div>
<div id = "hidden_option">
  <%= f.fields_for :student do |s| %>
  <%= s.label :rollNumber %>
  <%= s.text_field :rollNumber %>
  <% end %>
</div>


<%= f.check_box :role,  id: 'student-profile' %>
<%= label :role, 'Student' %>

<%= content_tag :div, class: 'js-dependent-fields', data: {'checkbox-id': 'student-profile', 'checkbox-value': true } do %>
  <%= f.fields_for :student do |s| %>
  <%= s.label :rollNumber %>
  <%= s.text_field :rollNumber %>
  <% end %>
<% end %>

  <div class="field">
    <%= f.label :password %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> characters minimum)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "off" %>
  </div>

  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "off" %>
  </div>

  <div class="actions">
    <%= f.submit "Sign up" %>
  </div>
<% end %>

<%= render "users/shared/links" %>
